import styles from './ThemeDarkIcon.module.css'

export const ThemeDarkIcon = () => {
  return (
    <button type="button" className={styles.normal}>
      {/* <img
        className={styles.icon}
        src={isActiveDark ? dark.activeIcon : dark.inactiveIcon}
        alt={dark.title}
      /> */}
    </button>
  )
}
